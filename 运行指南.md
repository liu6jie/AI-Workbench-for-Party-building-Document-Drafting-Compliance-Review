# 运行指南 & 技术栈说明

## 技术栈

### 后端 `backend/`

| 类别 | 技术 | 说明 |
| --- | --- | --- |
| Web 框架 | FastAPI 0.141 | 提供 REST API |
| ASGI 服务器 | Uvicorn 0.52（含 `standard` 扩展） | 本地开发用 `--reload` 热更新 |
| 数据校验 / 配置 | Pydantic 2.13 + pydantic-settings 2.14 | 请求体校验、`.env` 配置读取 |
| ORM / 数据库 | SQLAlchemy 2.0 + SQLite | 数据库文件落地在 `data/app.db`，无需额外装数据库 |
| 大模型调用 | openai SDK 2.50（接 DeepSeek 兼容接口） | `DEEPSEEK_API_KEY` / `DEEPSEEK_BASE_URL` 配置于 `backend/.env` |
| 其他 | python-multipart、python-dotenv | 表单/文件上传、环境变量加载 |

代码结构：
- `app/main.py`：FastAPI 入口 + CORS 配置
- `app/config.py`：`pydantic-settings` 读取的配置项
- `app/routers/materials.py`：材料生成/检查相关接口
- `app/services/generator.py`：拼装 Prompt、调用大模型生成文稿
- `app/services/checker.py`：合规检查逻辑
- `app/rules/engine.py`：规则引擎
- `app/prompts/templates.py`：各类材料的 Prompt 模板
- `app/models.py` / `app/db.py`：SQLAlchemy 数据模型与数据库连接

### 前端 `frontend/`

| 类别 | 技术 | 说明 |
| --- | --- | --- |
| 框架 | React 19 + TypeScript | |
| 构建工具 | Vite 8 | 开发服务器 + 生产构建 |
| UI 组件库 | Ant Design (antd) 6 + `@ant-design/icons` | 页面组件、图标 |
| 路由 | react-router-dom 7 | 多页面 / 起草向导流程 |
| HTTP 请求 | axios | 封装于 `src/api/client.ts` |
| 日期处理 | dayjs | 表单日期字段 |
| Markdown 编辑 | `@uiw/react-md-editor` | 文稿内容编辑/预览 |
| Lint | oxlint | `npm run lint` |

代码结构：
- `src/pages/draft/`：起草向导三步页面（选模板 / 填表单 / 生成与合规检查）
- `src/pages/history`、`knowledge`、`system`：历史记录、知识库、系统设置页面
- `src/components/MaterialFormConfig.ts`：5 类材料模板的表单结构配置
- `src/layouts/MainLayout.tsx`：整体布局（顶部导航 + 左侧菜单 + 内容区）
- `src/api/`：后端接口封装

### 部署配置（已就绪，暂未使用）

- `backend/render.yaml`：Render 平台后端部署配置
- `frontend/vercel.json`：Vercel 平台前端 SPA 路由重写配置

## 本地运行步骤

### 1. 启动后端

```powershell
cd backend
.\venv\Scripts\Activate.ps1        # 激活虚拟环境（首次需先 python -m venv venv && pip install -r requirements.txt）
uvicorn app.main:app --reload --port 8001
```

- 需要 `backend/.env` 中配置好 `DEEPSEEK_API_KEY`（已配置）
- 启动后访问 http://127.0.0.1:8001/health，看到 `{"status":"ok"}` 说明后端正常
- 端口可按需更改（例如遇到 `WinError 10013` 端口权限问题时换一个端口），但要同步修改前端的 `VITE_API_BASE_URL` 或 `src/api/client.ts` 里的默认地址

### 2. 启动前端

```powershell
cd frontend
npm install     # 首次运行需要
npm run dev
```

- 默认打开 http://localhost:5173（端口被占用时 Vite 会自动换到 5174 等）
- 前端通过 `src/api/client.ts` 中的 `baseURL` 连接后端，默认指向 `http://127.0.0.1:8001`

### 3. 使用

浏览器打开前端地址（如 http://localhost:5173），进入"材料智能起草"页面，按向导三步完成模板选择、表单填写、AI 生成与合规检查。后端接口地址不需要单独在浏览器打开，只是前端调用用的接口。

## 生产构建（可选）

```powershell
cd frontend
npm run build     # 产物在 frontend/dist
```
